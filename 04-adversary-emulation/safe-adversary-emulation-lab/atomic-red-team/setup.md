# Atomic Red Team Setup

The endpoint evidence shows `Invoke-AtomicTest` PowerShell module version `2.3.0` and an Atomic Red Team directory under `C:\AtomicRedTeam`.

Only selected, non-destructive tests were used. Atomic Red Team test definitions can contain strings that antivirus products flag. The project does not recommend disabling Defender globally; review selected tests and preserve protection.
